This folder should contain all the application 
specific workflows necessary for your UiPath project.

Each application should get its own folder (ie Chrome
or SAP), then from there, all files can be organized
into sub folders if desired.